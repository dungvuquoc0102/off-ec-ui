const config = {
  routes: {
    home: "/",
    //user
    users: "/users",
    //product
    products: "/products",
    productDetail: "/products/:slug",
    //auth
    login: "login",
    register: "register",
  },
};

export default config;
